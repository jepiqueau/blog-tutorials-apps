import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'typeorm-ionic7-react-sqlite-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
