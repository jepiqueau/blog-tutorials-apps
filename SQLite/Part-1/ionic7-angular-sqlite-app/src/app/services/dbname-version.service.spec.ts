import { DbnameVersionService } from './dbname-version.service';

describe('DbnameVersionService', () => {
  it('should create an instance', () => {
    expect(new DbnameVersionService()).toBeTruthy();
  });
});
