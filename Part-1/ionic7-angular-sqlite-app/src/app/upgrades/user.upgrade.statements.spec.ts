import { UserUpgradeStatements } from './user.upgrade.statements';

describe('UserUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new UserUpgradeStatements()).toBeTruthy();
  });
});
