import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeepq.ionic7.angular.videoplayer',
  appName: 'ionic7-angular-videoplayer-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
