import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeep.app.ionic7.angular.photoviewer',
  appName: 'ionic7-angular-photoviewer-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
